apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moviepilot.fullname" . }}-test-connection"
  labels:
    {{- include "moviepilot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moviepilot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
