apiVersion: v1
kind: Service
metadata:
  name: {{ include "moviepilot.fullname" . }}
  labels:
    {{- include "moviepilot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moviepilot.selectorLabels" . | nindent 4 }}
